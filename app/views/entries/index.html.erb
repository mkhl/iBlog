<h1>
  <%= @blog && @blog.title.present? ? @blog.title : 'Alle Einträge' %><br>
  <small>
    <% if @blog %>
      <%= content_for :title, "Alle Einträge in \"#{@blog.name}\"" %>
      <%= @blog.name %> von <%= @blog.author.name %>
    <% else %>
      <%= content_for :title, "Alle Einträge in allen Blogs" %>
      für alle Blogs
      <% if @author %>
      von <%= @author.name %>
      <%= content_for :title, " von #{@author}" %>
      <% end %>
    <% end %>
  </small>
</h1>

<%= render @entries %>

<%= paginate @entries %>

