<h1>Blogpost <%= @entry.id.nil? ? 'erstellen' : 'bearbeiten' %></h1>

<% form_for(@entry) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :excerpt %><br />
    <%= f.text_area :excerpt %>
  </p>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.label :author %><br />
    <%= f.text_field :author %>
  </p>
  <p>
    <%= f.submit( @entry.id.nil? ? 'Anlegen' : "Ändern" ) %>
  </p>
<% end %>

<% unless @entry.id.nil? %>
<%= link_to 'Show', @entry %> |
<% end %>
<%= link_to 'Back', entries_path %>
