<%= content_for :title, @entry.title %>
<%= render @entry %>
<ol class="comments unstyled">
  <% @comments.each do |comment| %>
     <%= render :partial => 'comments/comment', 
         :locals => {:comment => comment, :edit_comment => @edit_comment} 
%>
  <% end %>
</ol>
