<%= simple_form_for [blog, entry, comment] do |f| %>
  <%= f.input :content, :as => :text, :label => false, :input_html => { :class => "span6", :rows => 3 } %>
  <%= f.submit "kommentieren", :class => "btn" %>
<% end %>