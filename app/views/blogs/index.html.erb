<%= auto_discovery_link_tag :atom, blogs_path(:atom) %>

<h1>Listing blogs</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Owner</th>
    <th>Description</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%=h blog.name %></td>
    <td><%=h blog.owner %></td>
    <td><%=h blog.description %></td>
    <td><%= link_to 'Show', blog_entries_path(blog) %></td>
    <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
    <td><%= link_to 'Destroy', blog, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New blog', new_blog_path %>